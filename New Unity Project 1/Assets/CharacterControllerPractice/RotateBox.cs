using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class RotateBox : ScriptableObject {

	public float speed = 20.0F;
	public float gravity = 10.0F;
	private Quaternion turnDirection;

public void Turn(CharacterController controller, Transform transform)
{
	turnDirection.x = 15 * Time.deltaTime;	
		turnDirection.y = 30 * Time.deltaTime;
		turnDirection.z = 35 * Time.deltaTime; 
		turnDirection = transform.TransformDirection(turnDirection);
}

		
		
		//transform.Rotate (new Vector3 (15, 30, 35) * Time.deltaTime);
		
}
*/
