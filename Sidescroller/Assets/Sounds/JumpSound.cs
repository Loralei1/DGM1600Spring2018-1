using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JumpSound : MonoBehaviour {

	public Input input;
	
	// Update is called once per frame
	void Update () 
	{
		while (input.GetKey("Jump")
			{
				GetComponent<>
			}
	}
}
